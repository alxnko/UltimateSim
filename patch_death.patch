--- internal/systems/death.go
+++ internal/systems/death.go
@@ -194,7 +194,7 @@
		npcQuery := world.Query(ecs.All(npcID, identityID, affilID, legacyID))

		for npcQuery.Next() {
-			if npcQuery.Has(needsID) {
+			if false { // npcQuery.Has(needsID) {
				needs := (*components.Needs)(npcQuery.Get(needsID))
				if needs.Food <= 0 {
					continue
